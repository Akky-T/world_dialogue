<%= render "shared/second-header"%>

<%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class='sign-up-in-title'>Sign up</div>

  <div class="text-nickname">
    <div class='form-text-wrap'>
      <label class="form-text">Nickname</label>
    </div>
    <%= f.text_area :nickname, class:"input-default", size: "20x1", id:"nickname", placeholder:"ex. Tom ", maxlength:"40" %>
  </div>

  <div class="field-email">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field-password">
    <%= f.label :password %>
      <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
  </div>
  <%= f.password_field :password, autocomplete: "new-password", class: "password-box" %>

  <div class="field-password-confirmation">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="collection-gender">
    Gender
  </div>
  <%= f.collection_select(:gender_id, Gender.all, :id, :name, {}, {class:"select-box-gender", id:"user-gender"}) %>

  <div class="text-age">
    <div class='form-text-wrap'>
      <label class="form-text">Age</label>
    </div>
    <%= f.text_area :user_age, class:"input-default", size: "3x1", id:"user_age", placeholder:"26", maxlength:"3" %>
  </div>

  <div class="collection-nationality">
    Nationality
  </div>
  <%= f.collection_select(:nationality_id, Nationality.all, :id, :name, {}, {class:"select-box-nationality", id:"user-nationality"}) %>

  <div class="collection-occupation">
    Occupation
  </div>
  <%= f.collection_select(:occupation_id, Occupation.all, :id, :name, {}, {class:"select-box-occupation", id:"user-occupation"}) %>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
